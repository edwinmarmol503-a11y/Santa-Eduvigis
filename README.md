# Panaderia Santa Eduvigis - sitio estatico

IMPORTANTE: borra primero TODOS los archivos viejos del repo (o crea el repo
de nuevo) antes de subir esto. La vez pasada solo se reemplazaron algunas
paginas HTML pero el archivo assets/css/style.css se quedo con una version
vieja -> por eso se veian formas negras y el link "Saltar al contenido" sin estilo.

Sube TODO el contenido de esta carpeta a la RAIZ del repo (index.html arriba del todo).
Settings > Pages > Deploy from a branch > main > / (root). Repo debe ser Public.

Si tenias el repo ya creado: entra a assets/css/style.css en GitHub, dale
"Delete file", luego sube de nuevo TODA la carpeta assets completa.
